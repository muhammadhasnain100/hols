/** Referral integration — dedicated affiliate referral endpoints will be added later. */

export type AffiliateReferralsPlaceholder = {
  message: string;
};

export const affiliateReferralsIntegration: AffiliateReferralsPlaceholder = {
  message: "Referral API integration pending",
};
